let {
    insert,
    del,
    find,
    update
} = require("../../libs/mongo.js");

// insert("students",[{"name":"赵六"}]);

// del("week",{name:"laotian"});

// find("students",{"name":"张三","age":20}).then((result)=>{
//     console.log(result);
// });

// update("week", {name: "laotian"}, {name: "laoyao"});